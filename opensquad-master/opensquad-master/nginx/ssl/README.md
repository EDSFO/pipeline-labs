# SSL Certificates

Place your SSL certificates in this directory.

Required files:
- `cert.pem` — SSL certificate
- `key.pem` — SSL private key

Once certificates are added, uncomment the SSL configuration block in `nginx.conf`.
